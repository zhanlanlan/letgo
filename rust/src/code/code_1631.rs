// https://leetcode.com/problems/path-with-minimum-effort/

fn minimum_effort_path(heights: Vec<Vec<i32>>) -> i32 {
    let (mut min, mut max, mut mid) = (0, 1000000, (0 + 1000000) / 2);

    while min <= max {
        // mid = (min + max) / 2;


    }
    todo!()
}

#[cfg(test)]
mod test {
    use super::*;

    #[test]
    fn test_minimum_effort_path() {}
}
